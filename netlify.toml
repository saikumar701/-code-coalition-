[build]
  base = "client"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_BACKEND_URL = "https://code-coalition.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
